PORT=5000

# ── Supabase / Render deployment ─────────────────────────────────
# Paste your Supabase connection string here in Render env vars
DATABASE_URL=

# ── CORS ─────────────────────────────────────────────────────────
# Set to your Vercel frontend URL in production, e.g. https://sres.vercel.app
FRONTEND_URL=http://localhost:3000

# ── Local development (used when DATABASE_URL is empty) ──────────
DB_USER=postgres
DB_HOST=localhost
DB_NAME=sres_db
DB_PASSWORD=your_password
DB_PORT=5432

JWT_SECRET=supersecure_sres_jwt_secret_key_123
